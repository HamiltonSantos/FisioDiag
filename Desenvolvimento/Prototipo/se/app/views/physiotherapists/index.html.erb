<h1>Fisioterapeutas</h1>

<table class="table">
  <tr>
    <th>Nome</th>
    <th>Login</th>
    <th>Senha</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @physiotherapists.each do |physiotherapist| %>
  <tr>
    <td><%= physiotherapist.nome %></td>
    <td><%= physiotherapist.login %></td>
    <td>******</td>
    <td><%= link_to 'Detalhes', physiotherapist, :class => 'btn btn-info' %></td>
    <td><%= link_to 'Editar', edit_physiotherapist_path(physiotherapist), :class => 'btn btn-warning' %></td>
    <td><%= link_to 'Excluir', physiotherapist, method: :delete, data: { confirm: 'Você tem certeza?' }, :class => 'btn btn-danger' %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Fisioterapeuta', new_physiotherapist_path, :class => 'btn' %>
